# Configs

Graph-generation YAML files in this directory feed `data/generate_graphs.py`.
Each file names an experiment (e.g. `community_only`) and the graph family it
produces; pass its basename to `--config`.
